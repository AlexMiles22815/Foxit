return {

    softScriptErrors = true,   -- // Do not crash the engine when task / script error happens
    skipIntro = true,           -- // Self expl
    hideEngineLogs = true      -- // Hide Engine's internal logs

}